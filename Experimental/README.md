# Experimental

Parked probe files from earlier research campaigns. These contain incomplete
proofs (`sorry`) and are NOT imported by any certified result. They are kept
as a record of investigated-and-parked directions. The certified development
lives entirely under `RHFormalization/`; the RH endpoint
`RH_from_pairedTransform_only` checks with `#print axioms` to exactly
`[propext, Classical.choice, Quot.sound]`.
